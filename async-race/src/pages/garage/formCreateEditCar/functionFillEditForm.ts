// import { Car } from "../../../utils/types";

// export function fillEditForm(formEditCar: HTMLDivElement, car: Car) {
//   const [inputText, inputColor, buttonEdit] = Array.from(formEditCar.children);
//   if (
//     inputText instanceof HTMLInputElement &&
//     inputColor instanceof HTMLInputElement &&
//     buttonEdit instanceof HTMLButtonElement
//   ) {
//     inputText.value = car.name;
//     inputText.classList.remove("disabled");
//     inputText.disabled = false;
//     inputColor.value = car.color;
//     inputColor.classList.remove("disabled");
//     inputColor.disabled = false;
//     buttonEdit.disabled = false;
//     buttonEdit.classList.remove("disabled");
//   }
// }
